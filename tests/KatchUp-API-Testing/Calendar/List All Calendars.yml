info:
  name: List All Calendars
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5050/api/calendars
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
